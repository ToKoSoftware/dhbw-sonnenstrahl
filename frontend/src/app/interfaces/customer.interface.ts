export interface CustomerData {
  id: string;
  firstName: string;
  lastName: string;
  street: string;
  streetNumber: string;
  postcode: string;
  city: string;
  is_active: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
}
