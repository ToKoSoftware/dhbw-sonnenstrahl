export interface OrderData {
  id: string;
  firstName: string;
  lastName: string;
  street: string;
  streetNumber: string;
  postcode: string;
  city: string;
  referrer: string;
  planId: string;
  consumption: number;
  is_active: boolean;
}
